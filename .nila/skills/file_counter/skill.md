# File Counter

Count files in a directory by extension. Use the `count_files.ts` script to analyze file distributions.

The script accepts:
- Directory path (defaults to current directory)
- Optional file extension filter

Example usage: Count all TypeScript files in the `src/` directory.
